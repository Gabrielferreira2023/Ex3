#include <stdio.h>

int main() {
    int soma = 0;
    int contador = 1;
    int numero = 0;
    
    while (contador <= 50) {
        numero += 2;
        soma += numero;
        contador++;
    }
    
    printf("A soma dos 50 primeiros números pares é: %d\n", soma);
    
    return 0;
}
